{
  "name": "careernavigator-deploy",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "npm install --prefix backend",
    "build": "npm run build --prefix backend",
    "start": "npm run start --prefix backend"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
